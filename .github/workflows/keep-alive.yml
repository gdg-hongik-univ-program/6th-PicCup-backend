name: Keep Render Backend Alive

on:
  schedule:
    # 매 10분마다 실행
    - cron: '*/10 * * * *'
  # GitHub Actions 탭에서 수동 실행 지원
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health API
        run: curl -s https://piccup-api.onrender.com/health